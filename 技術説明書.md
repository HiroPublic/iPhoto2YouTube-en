# Local Video Uploader 技術説明書

## 1. 構成概要

本アプリは次の 2 層で構成される。

- SwiftUI ベースの macOS ネイティブ UI
- Python 製 CLI とローカルデータ管理層

Swift 側は UI、操作フロー、ローカル表示状態を担当し、実際の YouTube 操作や履歴保存の中心は CLI と SQLite を通じて行う。

## 2. 主要ディレクトリ

- `Sources/iPhoto2YouTubeNativeApp`
  SwiftUI アプリ本体
- `src/iphoto2youtube_cli`
  Python CLI、本体ロジック、設定、履歴保存
- `Tests/iPhoto2YouTubeNativeAppTests`
  Swift 側テスト
- `script/build_and_run.sh`
  macOS アプリのビルドと `dist` へのステージ

## 3. Swift 側の役割

### 3.1 `ContentView.swift`

- 画面レイアウトを定義する
- Shared Metadata 入力 UI を持つ
- `HistoryOptionsPopoverButton` により履歴選択と削除 UI を提供する
- Upload / Recent History / History Calendar / Photos を切り替える

### 3.2 `AppViewModel.swift`

- UI 状態の単一管理点
- 認証状態、アップロード状態、履歴表示状態を保持する
- CLIService を呼び出して CLI と通信する
- メタデータ履歴の保存、削除、再読込を制御する
- 起動時に `upload_history.db` からメタデータ履歴をバックフィルする

### 3.3 `Models.swift`

- UI モデルとデータ保存ロジックを定義する
- `HistoryCalendarRepository` で `history_calendar.db` を扱う
- `MetadataHistoryStore` で `metadata_history.json` を扱う
- `LedgerSuggestionLoader` で履歴候補を組み立てる

## 4. CLI 側の役割

### 4.1 `src/iphoto2youtube_cli/config.py`

- サポートディレクトリと各ファイルパスを定義する
- `config.json` の設定値を読み込む

### 4.2 `src/iphoto2youtube_cli/storage.py`

- `upload_history.db` と `management.db` を管理する
- アップロード結果、実行ログ、API クォータ情報を保存する
- `ledger.csv` の元になる履歴データを提供する

### 4.3 `CLIService.swift`

- Swift から CLI コマンドを起動するアダプタ
- 認証、履歴取得、アップロード、検証などを JSON 入出力で扱う

## 5. メタデータ履歴の技術仕様

### 5.1 保存先

手動履歴は `./.iphoto2youtube/metadata_history.json` に保存する。

保存対象:

- places
- eventNames
- participantNames
- playlists
- cameraModels

加えて、再表示を抑止する削除済みリストも保持する。

- suppressedPlaces
- suppressedEventNames
- suppressedParticipantNames
- suppressedPlaylists
- suppressedCameraModels

### 5.2 入力コミット時の保存

以下の入力はコミット時に `MetadataHistoryStore.remember(...)` を呼ぶ。

- Location
- Event
- Participants
- Playlist
- Camera

`Participants` はカンマ区切りや改行を分割し、個別名で保存する。

### 5.3 バックフィル

起動時に `AppViewModel` が `MetadataHistoryStore.backfillFromUploadHistory()` を実行する。

優先順:

1. `upload_history.db`
2. `ledger.csv`

`upload_history.db` が存在する場合は、`uploaded_at DESC, id DESC` で新しい順に読み込む。

### 5.4 履歴候補の組み立て

`LedgerSuggestionLoader.loadOptions(...)` は次をマージして候補を返す。

1. `metadata_history.json` の手動履歴
2. `upload_history.db` または `ledger.csv` 由来の履歴
3. 既定候補

表示順は新しい順で、削除抑止リストに含まれる値は除外する。

### 5.5 履歴削除

履歴ポップオーバーの削除ボタンは `AppViewModel.deleteCommon*History(...)` を経由して `MetadataHistoryStore.remove(...)` を呼ぶ。

削除時の挙動:

- 手動履歴配列から対象値を除去する
- 対応する suppressed 配列へ追加する
- 再読込後の候補一覧から非表示にする

同じ値を再度コミットした場合は suppressed 配列から除外され、候補へ復帰する。

## 6. 履歴カレンダー

`HistoryCalendarRepository` は `history_calendar.db` を管理し、以下を保存する。

- 日別アップロード件数
- 日別削除件数
- 手動アップロードマーク
- 手動削除マーク
- 日別メモ
- Photo Library 削除履歴

## 7. ビルドと配布

### 7.1 Swift Package

`Package.swift` は macOS 14 以上を対象とする executable target を定義する。

### 7.2 Xcode ビルド

開発時のステージングには `script/build_and_run.sh` を使う。

主な処理:

1. `xcodebuild` で Debug ビルド
2. 生成された `.app` を `dist/Local Video Uploader.app` へ `ditto` でコピー
3. 必要に応じて起動

### 7.3 反映時の注意

ソース変更後に古い `dist/Local Video Uploader.app` を起動すると、新 UI が反映されないことがある。UI 確認時は必ず最新ビルドをステージした `.app` を再起動すること。

## 8. テスト

Swift 側の確認は `swift test` を用いる。

確認対象:

- マニフェスト生成
- 履歴候補の読み込み
- `metadata_history.json` の保存
- `upload_history.db` からのバックフィル
- 履歴削除と suppressed 動作

運用上、環境によってはビルド完了後にテストランナー出力が返らず待機状態になる場合がある。この場合でもビルドとリンク通過は別途確認できる。
