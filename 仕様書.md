# Local Video Uploader 仕様書

## 1. 目的

Local Video Uploader は、ローカル動画や Photo Library から取り出した動画を、利用者本人の YouTube チャンネルへ手動でアップロードするための macOS デスクトップアプリである。

本アプリは個人利用を前提とし、外部サーバーを介さず、ユーザー操作に基づいてのみ動作する。

## 2. 対象環境

- macOS 14 以降
- YouTube Data API を利用可能な Google アカウント
- ローカルの Python 実行環境と CLI 実行ファイル

## 3. 主要機能

### 3.1 認証

- Google OAuth 2.0 により認証する
- 認証済みチャンネル情報を表示する
- サインアウトと再認証を行える

### 3.2 動画アップロード

- ローカルファイルを複数選択できる
- Dry Run を実行できる
- アップロード前に確認ダイアログを表示する
- アップロード後に結果と検証情報を表示する

### 3.3 共通メタデータ

共通メタデータとして以下を保持する。

- Location
- Event
- Participants
- Camera
- Playlist
- Notes
- Library Name
- Timezone
- OffsetTimeOriginal
- Video Visibility
- Playlist Visibility

共通メタデータは空欄の動画ドラフトへ一括適用できる。

### 3.4 メタデータ履歴

履歴管理の対象項目は以下である。

- Location
- Event
- Participants
- Playlist
- Camera

履歴仕様:

- 手入力値はコミット時に永続保存する
- 既存のアップロード履歴から起動時に自動バックフィルする
- 表示順は新しいものが上とする
- 履歴ポップオーバーから値を選択できる
- 履歴ポップオーバーから値を削除できる
- 削除した値は過去のアップロード履歴に残っていても再表示しない

### 3.5 Photo Library 連携

- 指定日付の動画を読み込める
- 読み込んだ動画を条件別に選択・キュー追加できる
- iPhoto 上の動画削除フローを持つ
- 削除結果を履歴カレンダーへ反映する

### 3.6 履歴表示

- Recent History 画面で過去のアップロード履歴を表示する
- History Calendar 画面で日別のアップロード件数と削除件数を表示する
- 日付ごとの手動マークとメモを保持する

## 4. 画面構成

### 4.1 左ペイン

- Connection
- Authenticated Channel
- YouTube Data API
- Shared Metadata

### 4.2 右ペイン

以下の画面を切り替える。

- Upload
- Recent History
- History Calendar
- Photos

## 5. データ保存

サポートディレクトリは通常 `./.iphoto2youtube` を使用する。

主な保存ファイル:

- `config.json`: ユーザー設定
- `client_secret.json`: OAuth クライアント設定
- `token.json`: OAuth トークン
- `upload_history.db`: アップロード履歴
- `management.db`: 動画管理データ
- `history_calendar.db`: カレンダー集計と削除履歴
- `ledger.csv`: 履歴エクスポート CSV
- `metadata_history.json`: 手動履歴と削除抑止情報
- `upload_limit_state.json`: API 制限到達時刻の推定値

## 6. 履歴削除仕様

履歴から削除した項目は、単に一覧から消すだけではなく、抑止情報として保存する。

対象:

- 手動追加された履歴
- `upload_history.db` 由来の履歴
- `ledger.csv` 由来の履歴

期待動作:

- 削除した値は直後にポップオーバーから消える
- 再起動後も非表示を維持する
- 同じ値を再度手入力または選択した場合は履歴へ復帰する

## 7. 非機能要件

- すべてのアップロード処理はユーザー起点であること
- OAuth パスワードを保存しないこと
- ローカル保存データは外部送信しないこと
- YouTube API 派生データは最大 30 日保持すること

## 8. 既知の運用上の注意

- ビルド済み `.app` を使う場合は、ソース変更後にアプリを再ビルドして反映する必要がある
- テスト実行環境によっては `swift test` がビルド後に待機状態になる場合がある
